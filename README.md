# A2_PE6201_GRP4

Group 4 A2 project for PE6201.

Version 1.1 keeps Xiaohua's v1.0 data-access layer and adds LI ZIHAO's shared
schemas, parser, dependency checks, provider-neutral backend seam, manual agent
loop, sequential/parallel execution switch, trace and smoke tests.

Run all current tests from this directory:

```text
python -m unittest discover -s tests -v
```

See `docs/HANDOFF_LI_ZIHAO.md` for module boundaries and integration inputs.
