# A2_PE6201_GRP4

Group 4 A2 project for PE6201.

Version 1.2 combines Xiaohua's seven Problem A tools and registry with LI ZIHAO's
shared schemas, parser, dependency checks, provider-neutral backend seam, manual
agent loop, sequential/parallel execution, trusted write-tool integration, trace,
and automated tests.

Run all current tests from this directory:

```text
python -m unittest discover -s tests -v
```

See `docs/HANDOFF_LI_ZIHAO.md` for module boundaries and
`docs/V1.2_INTERFACE_REFINEMENTS_BILINGUAL.md` for the current interface notes.
